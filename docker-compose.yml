web:
  build: .
  ports:
    - "8080:80"
  links:
    - mysql
  env_file:
    - env
  volumes:
    - .:/var/chippin
    - ./src/var/connect:/var/www/htdocs/var/connect

mysql:
  image: mysql:5.6.23
  env_file:
    - env
